import { RewardProps } from "components/[guild]/RoleCard/components/types"

// We actually don't render this component anywhere, so just returning a fragment here - we don't want to refactor the other rewards now...
const FarcasterChannelReward = (_: RewardProps) => <></>

export default FarcasterChannelReward
