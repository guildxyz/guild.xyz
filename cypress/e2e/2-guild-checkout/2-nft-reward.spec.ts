describe("nft reward", () => {
  it.skip("should be able to deploy an nft contract", () => {
    expect(true).to.equal(true)
  })

  it.skip("should be able to collect an nft", () => {
    expect(true).to.equal(true)
  })
})
